# QA findings log (lead) — updated during integration

## Round 1 (after wave 1 + chapters 01–06)
- [gl/tesserae] Scattered tiles (tess < .5, p3 0) read as large confetti near the camera (Ch 02 p 0, hero p .9): the scatter shell r 6–9 around z −4 puts tiles within 2–3 units of a camera at z 7. Fix: scattered tiles must read as distant glints — shell radius ≥ 14 (and never within 6 units of the camera), tile scale × .5 when tess < .5, alpha × smoothstep(0,.6,tess) so a low `tess` is a faint sparkle, not confetti. Keep the assembled formations untouched.
- [gl/stars + ch/03] The GL gold constellation (set 1) and the DOM constellation() do not coincide (GL fits the full centre-70% box, DOM keeps MED_ASPECT). Fix in stars.ts: use the same aspect-true fit as constellations.ts (export a shared `fitOutline(w,h)` from constellations.ts if needed — art file, lead permits stars agent to add an export there).
- [ui/rail] Rail labels (`04 · THE TENTH DAWN`) sit at x 68–190 px / y 24–44% and collide with every chapter's headline / storyteller stack at left 7%. Fix: set the label vertical (writing-mode: vertical-rl; rotate 180°) hugging the rail line on its right, 9 px mono, so it never crosses the content column; keep the 3 s hold.
- [ch/04 + ch/05 + gl/tesserae] The mosaic Mediterranean lies flat and reads as a thin oblique band from the Ch 04/05 camera (camY 1.4, tilt −.08, z 2). The bible wants it legible as the Mediterranean outline. Fix: raise/steepen the Ch 04 camera from p .5 (camY ≈ 3.4, camTilt ≈ −.42, camZ ≈ 3, looking down at the island anchor x 5.2, z −9) and hold that framing through Ch 05 (its p 0 must equal Ch 04's p 1), re-registering the routes plate; alternatively tilt the formation 20° toward the camera in tesserae-formations (aUp for the island). Prefer the camera.
- [ch/02] With the scatter fix, verify the frieze + ember at p 0 read cleanly; the storyteller stack at p .5 shows only the shatter lines (fine).
- [ch/03] Label "HOUSING & REAL-ESTATE PRESSURES" sits on a hairline; nudge.
- [engine] Done by lead: seam-based blend for films (smoothstep .86→1), `?p=` film-aware, override aliasing fixed, kf/kfRGB helpers, `.tess-out:not(.is-go)` gate, `flash-2` keyframes, `.glyph` defaults, motion toggle persisted, mosaic sweep wired, `world.layer()/world.tesserae`.

## Round 1 — mobile (390×844)
- [ch/03 + gl/stars] Portrait: the GL outline spans the full frame while the DOM constellation is small and centred; labels `GEOPOLITICAL TENSIONS…`, `HOUSING & REAL-ESTATE…` overflow the right edge. Fix: same fit for GL and DOM (see above); on portrait, labels ≤ 2 lines, end-anchored east labels flipped to start-anchor when near the right edge, or shorten via `data-short` labels (e.g. `GEOPOLITICS`, `HOUSING`) at < 820 px.
- [ui/veil + ch/05] shots/qa/mobile-unity-05.png: at Ch 05 p .5 (veil should be 1 = off-stage) the mobile moiré cloth covers the lower 60% of the frame with vertical stripes and shows the Kalyptein line; the storyteller/forum stack is not visible. Verify the mobile branch reads the phase (f = veil mod 1, hidden when f < .02 or > .98) and that `data-veil-line` is removed after Ch 04; check Ch 05's mobile mood actually reaches veil 1 by p .15.
- [ui/rail] Portrait: the bottom-right `05 / 14 · 00:15` and the paradise `— · STAY TODAY` label collide with the bottom-centre REGISTER pill. Fix: on < 600 px stack the corner labels above the pill's safe area (bottom: calc(env(safe-area-inset-bottom) + 4.2rem)) or move them top-right under the menu ring; hide the chapter count if it cannot fit.
- [ch/06] Portrait: the mosaic tiles (formation 1) show at bottom-right behind the pill — acceptable, but the column's disc glyph overlaps the `DAY 02` head; keep the disc left of the head with a 12 px gap.

## Round 1 — full site (after all 14 chapters)
- [engine, done] camYaw stays the Three.js convention (positive turns left); film p from --film-len (Ch 14's footer no longer stretches p); blend at the seam (.86→1) for films; [hidden] rule.
- [gl/tesserae] (a) scatter = faint distant glints (see above); tess 0 must be invisible whatever tessSpread is (Ch 11 p 1 / Ch 12 / Ch 13 declare tess 0 with spread 1). (b) shots/qa/desktop-sunrise-0.png: a beige rectangle floats in the sky at ≈(740,280) — surplus instances parked at a centroid are visible as one block during the 3→4 morph; surplus/tint-0 instances must have alpha 0 in every formation and every fractional morph. (c) Island formation reads as a thin oblique band from the Ch 04/05 camera: tilt formation 1 ≈ 25° toward the camera about its own x-axis (keep its anchor) so the outline is legible, and keep `anchors.island` correct for Ch 05's routes plate. (d) Hand assembly: bring the fist→open assembly earlier so ≈50% of the hand is on stage at Ch 11 p .5 (chapter drives tess; coordinate: layer just needs the stagger to be visible early, no change if the chapter shifts its tess window).
- [gl/sea] shots/qa/desktop-register-05.png and desktop-sunrise-0.png: with the camera at y .7 / z −12 the sea disc's far rim is a hard line ~4 vh ABOVE the true eye-level horizon and the star (sunY .2) renders below that line on the water. The rim must sit at eye level for any camera height (extend the disc toward the far plane / fade the last rings into skyBottom) so nothing above the sea shows a ledge. Verify at `?chapter=register&p=.5` and `?chapter=sunrise&p=0` (1440×900 and 390×844).
- [gl/sky] Star mode (sunHeat < .35): the wide halo term makes a sun-sized grey glow around the star (Ch 12 p .7, Ch 03 p .7). Gate the halo by sunHeat (×.12 in star mode) so the star reads as a point with delicate spikes on black. Verify `?chapter=homer&p=.7` and `?chapter=stars&p=.7`.
- [gl/stars] Set 1 must coincide with the DOM constellation(): use the same aspect-true fit (import/export a shared fitOutline(w,h) from src/art/constellations.ts — adding that export is permitted); drop the closing Malta→Gibraltar edge if the DOM omits it; Orion/Bear placement unchanged. Verify `?chapter=stars&p=.5` at 1440×900 and 390×844 — the gold GL lines must sit on the DOM hairlines.
- [ui/veil] (a) shots/qa/mobile-hand-09.png, mobile-register-05.png, mobile-homer-09.png: on < 820 px the moiré cloth's torn halves stay on stage while the veil is parked (veil 3 → f 0) — hide the cloth (both halves, both modes) whenever f < .02 or f > .98, including the torn state, and never show the tear pieces unless --tear-now > 0 AND the cloth is on stage. (b) The carried line ('Kalyptein…') leaks into Ch 07/08/09/12 crossings (desktop-eleven-09.png): only render `data-veil-line` while the cloth is on stage for the crossing in which it was set; clear the text when the cloth hides. Verify at 390×844 `?chapter=register&p=.5` and 1440×900 `?chapter=eleven&p=.9`.
- [ui/rail] (a) Chapter labels collide with headline/storyteller blocks at left 7% in most chapters (hero, warning, unity, paradise, eleven, remains…): render the label VERTICAL along the rail line (writing-mode: vertical-rl, rotated so it reads bottom-to-top, hugging the line's right side, --fs-index, --fg-muted) so it never enters the content column; keep the 3 s hold + hover. (b) Portrait: the bottom-right count/clock and 'STAY TODAY' collide with the bottom-centre REGISTER pill (mobile-paradise-05.png): on < 600 px move the corner labels to sit above the pill (bottom: calc(env(safe-area-inset-bottom) + 4.4rem)) or top-right under the menu ring. Verify hero/paradise/eleven at both sizes.
- [ch/03] After the stars fit fix, confirm coincidence; nudge 'HOUSING & REAL-ESTATE PRESSURES' off its hairline; portrait: labels overflow the right edge (mobile-stars-05.png) — clamp label boxes inside the frame, flip anchors near the right edge, and use shorter portrait labels where needed (e.g. GEOPOLITICS · HOUSING) without changing the desktop copy.
- [ch/04] Remove `data-veil-line` from #veil in onLeave (both directions) and whenever p < .84, so the line cannot leak; keep everything else.
- [ch/08] The engine no longer blends from .62 (films hand over at .86→1): remove the compensation inside mood() and return the §6.8 table directly (tear at p .92–.96, yaw locks at .33/.50/.68/.85). camYaw is the Three.js convention (positive turns LEFT): mirror the EAST/WEST screen-x computation and the ring's counter-rotation so labels match the world. At each lock the panel must show its body (chips/ladder/six Ps docked) — desktop-rudder-05.png shows an empty framed card with only the title while the Ps orbit; dock them within ~6% of p after the lock.
- [ch/11] Bring the hand assembly earlier so at p .5 the hand is clearly forming (tess 0→1 over p .38–.66, formation 2→3 over .42–.70); p .9 is already superb — keep it. Keep tess at 1 through p 1 and let Ch 12 own the dissolve (Ch 12 p 0 declares tess .6→0 — fine).
- [ch/12] Seam with Ch 13: at p 1 set the star to (0, 1.05, −22) (above eye level for the low camera) — Ch 13 p 0 will match.
- [ch/13] The star must sit above the eye-level horizon: sun (0, 1.05, −22) throughout (was .2/.15/.1) — coordinate p 0 with Ch 12 p 1 and p 1 with Ch 14 p 0. Portrait (mobile-register-05.png): the raft floats mid-sky — pin the raft's waterline to var(--horizon-now) on all sizes.
- [ch/14] (a) The Stage now gives film p directly — remove the travelScale remap (URL p == film p; the footer scrolls after the film with p held at 1). (b) The sunrise must be visible from the p .35–1 camera (camY 2→3.2, tilt .07): put the sun far away — sunZ −60, sunY from camY − .6 (p 0, just under the horizon) → camY + .6 (p .35) → camY + 2.4 (p .6–1), sunRadius ≈ 3.6 so its apparent size matches the hero; p 0 star = (0, 1.05, −22) to match Ch 13, then it recedes to z −60 as it warms (heat 0→1). The tesserae path/disc follow the sun. (c) Freeze: mosaic .55 (not .7), fade the storyteller stack out by p .96 so the frozen frame holds only 'I lived.', the couplet and the gold pill; lighten the footer's paper wash accordingly and make sure the footer columns are legible. Verify `?chapter=sunrise&p=0/.35/.6/1` and the footer below.

## Round 1 — resolved (lead verification, 2026-09-05)
Landed from the fix agents: sea (horizon ledge gone — the rim sits at eye level at every camera height), sky (star-mode halo gated: a point with spikes, not a grey blob), stars (GL constellation now uses the shared `fitOutline` from src/art/constellations.ts and sits on the DOM hairlines), rail (vertical chapter labels — no more collisions with the content column; portrait corners cleared of the pill), tesserae (scatter is distant glints, tess 0 truly invisible, island slab tilted 25° and legible), veil (parks off-stage, no stale line), ogygia (veil line removed on leave), rudder (panel bodies dock at each lock), stars/03 (labels clamped).
Applied by the lead after the agents ran out of credits: ch/12 + ch/13 star anchor (0, 1.05, −22) so the bearing star sits above the eye-level horizon; ch/13 mobile raft pinned to `--horizon-now`; ch/14 travelScale removed (the Stage gives film p), sun moved far and high (z −60, radius 3.6) so the sunrise is visible from the risen camera, frozen mosaic settled to .3 with a light-on-water halo behind the couplet and a stronger footer wash; ch/11 spread closes earlier and the tesserae scatter shell now draws IN as the field assembles (`near = mix(1, .26, smoothstep(.05,.75,tess))`) so the hand reads as a converging swarm; NullWorld added for DOM-only mode (no chapter can dereference a null world); `?reduced` and `?nogl` harness flags; `[hidden]`, aria-hidden on the last decorative SVGs.

## Open (next round)
- Reduced-motion stack: a few chapter blocks overlap at section boundaries (shots/strip-reduced-desktop.jpg) — the static stacks need a `min-height` / flow pass.
- Mobile: re-walk every chapter at 390×844 after the rail/veil fixes (only spot checks were re-run).
- Ch 05: the tilted island runs off the right edge at 1440×900 — consider camX or the anchor.
- Ch 03: verify the GL/DOM constellation coincidence at portrait after the shared fit.
- Real-device iOS check (sticky pins, 100dvh, the bottom pill), Lighthouse, and the two solar times (16:56 / 06:51) against a calculator for Valletta.
